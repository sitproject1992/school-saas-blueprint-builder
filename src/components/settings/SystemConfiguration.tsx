import React from 'react';

const SystemConfiguration: React.FC = () => {
  return (
    <div>
      <h2 className="text-xl font-bold mb-4">System Configuration</h2>
      <p>
        This section will contain global settings that apply to all schools.
        For example, you could have settings for enabling or disabling certain
        features across the entire application.
      </p>
    </div>
  );
};

export default SystemConfiguration;
